const DB_USER = ""; //Aquí se agrega el usuario
const DB_PASSWORD = ""; //Aquí se agrega la contraseña de la BD
const DB_HOST = ""; //Aquí se coloca el servidor de la DB
const DB_NAME = ""; //Aquí se agrega el nombre de la base de datos.
const DB_PORT = ""; //Aqui se agrega el puerto de la DB

const API_VERSIOM = "";
const IP_SERVER = "";

module.exports = {
    DB_HOST,
    DB_NAME,                //Se exporta el archivo con las variables creadas
    DB_PASSWORD,
    DB_USER,
    DB_PORT,
    API_VERSION,
    IP_SERVER
}